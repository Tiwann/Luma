if(NOT TARGET rgfw)
    add_library(rgfw STATIC rgfw/src/rgfw.c)
    add_library(RGFW::RGFW ALIAS rgfw)
    target_include_directories(rgfw PUBLIC rgfw/include)
    if(LUMA_ENGINE_BUILD_VULKAN)
        target_link_libraries(rgfw PUBLIC Vulkan::volk)
        target_compile_definitions(rgfw PUBLIC RGFW_VULKAN RGFW_USE_VOLK)
    endif ()
endif ()

set(SPIRV_REFLECT_EXECUTABLE OFF)
set(SPIRV_REFLECT_STATIC_LIB ON)
set(SPIRV_REFLECT_BUILD_TESTS OFF)
set(SPIRV_REFLECT_ENABLE_ASSERTS ON)
set(SPIRV_REFLECT_ENABLE_ASAN OFF)
set(SPIRV_REFLECT_INSTALL OFF)
add_subdirectory(spirv-reflect)